version: "3.8"

services:
  mongo:
    image: mongo:7.0
    container_name: code-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27020:27017"
    volumes:
      - mongo-data-rollerite:/data/db

volumes:
  mongo-data-rollerite: